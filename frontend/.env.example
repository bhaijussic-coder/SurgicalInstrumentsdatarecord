VITE_API_BASE_URL=/api
VITE_API_PROXY_TARGET=http://127.0.0.1:5000
# In local development, Vite proxies /api requests to VITE_API_PROXY_TARGET.
# In production, set VITE_API_BASE_URL to the deployed backend API base URL if it is not served from the same origin.
